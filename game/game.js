const init = () => {
  const soundGreen = new audio();
  const soundRed = new audio();
  const soundYellow = new audio();
  const soundBlue = new audio();
  const wrong = new audio();

  new.addEventListener("click", (e) => {
    
  });

  init();
};
